<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="16dp"
    android:paddingHorizontal="8dp">

    <!-- Nombre del emprendimiento -->
    <TextView
        android:id="@+id/tvEmprendimientos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp"
        android:text="Emprendimiento"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold" />

    <!-- Contenedor de publicaciones con overlay -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- RecyclerView de publicaciones -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_posts"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:overScrollMode="never"
            android:clipToPadding="false"
            android:gravity="center_horizontal"
            android:paddingHorizontal="16dp"
            tools:listitem="@layout/item_publicaciones_perfil" />

        <!-- Layout de placeholders cuando no hay publicaciones -->
        <LinearLayout
            android:id="@+id/layoutEmptyPosts"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingHorizontal="16dp"
            android:visibility="gone">

            <!-- Recuadro 1 -->
            <LinearLayout
                android:layout_width="100dp"
                android:layout_height="140dp"
                android:layout_marginEnd="12dp"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/bg_placeholder_publicacion">

                <ImageView
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_vacio_publicacion"
                    app:tint="@color/darker_gray" />
            </LinearLayout>

            <!-- Recuadro 2 -->
            <LinearLayout
                android:layout_width="100dp"
                android:layout_height="140dp"
                android:layout_marginEnd="12dp"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/bg_placeholder_publicacion">

                <ImageView
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_vacio_publicacion"
                    app:tint="@color/darker_gray" />
            </LinearLayout>

            <!-- Recuadro 3 -->
            <LinearLayout
                android:layout_width="100dp"
                android:layout_height="140dp"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/bg_placeholder_publicacion">

                <ImageView
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_vacio_publicacion"
                    app:tint="@color/darker_gray" />
            </LinearLayout>
        </LinearLayout>

        <!-- Etiqueta de +N publicaciones -->
        <TextView
            android:id="@+id/tvMasPublicaciones"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_marginBottom="8dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_etiqueta_mas_publicaciones"
            android:paddingHorizontal="8dp"
            android:paddingVertical="2dp"
            android:text="+5"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:textSize="12sp"
            android:visibility="gone" />
    </FrameLayout>

</LinearLayout>
